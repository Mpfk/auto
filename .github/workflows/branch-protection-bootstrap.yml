name: Branch Protection Bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  apply:
    runs-on: ubuntu-latest
    env:
      ADMIN_TOKEN: ${{ secrets.GH_ADMIN_TOKEN }}
    steps:
      - name: Ensure admin token exists
        run: |
          if [ -z "$ADMIN_TOKEN" ]; then
            echo "GH_ADMIN_TOKEN secret is required to apply branch protection."
            exit 1
          fi

      - name: Apply branch protection to main
        uses: actions/github-script@v7
        env:
          GH_TOKEN: ${{ secrets.GH_ADMIN_TOKEN }}
        with:
          github-token: ${{ secrets.GH_ADMIN_TOKEN }}
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const branch = 'main';

            await github.request('PUT /repos/{owner}/{repo}/branches/{branch}/protection', {
              owner,
              repo,
              branch,
              required_status_checks: {
                strict: true,
                contexts: [
                  'Workflow Policy / policy',
                  'Test Suite / test',
                  'Conventional Commits Check / check-commits'
                ]
              },
              enforce_admins: true,
              required_pull_request_reviews: {
                required_approving_review_count: 1,
                dismiss_stale_reviews: true,
                require_code_owner_reviews: false
              },
              restrictions: null,
              allow_force_pushes: false,
              allow_deletions: false,
              required_conversation_resolution: true
            });

            core.info('Branch protection updated for main.');
